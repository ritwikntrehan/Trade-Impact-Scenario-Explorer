# Trade Impact / Tariff Scenario Explorer

Static-first Next.js prototype demonstrating first-order tariff scenario exposure analysis using curated local JSON sample data.

## Run

```bash
npm install
npm run dev
```

## Build static export

```bash
npm run build
```

The app is configured with `output: "export"` for static deployment.
